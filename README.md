# Excelweb React App

This repository contains a React component for date calculations.

## Setup

1. Install dependencies: `npm install`
2. Run development server: `npm start`

## Component

The `DateForm` component provides date parsing and calculations for an app dealing with sick leave and benefits.
